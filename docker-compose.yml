services:
  mongo:
    image: mongo:8.0.4
    container_name: discord_rag_mongo
    ports:
      - "27017:27017"
    volumes: 
      - mongo_data:/data/db

volumes:
  mongo_data:
    name: discord_rag_mongo_data